1. git init
2. git status
3. git add file name
4. git add --all
5. git commit -m "msg"
6. git remote add origin url
7. For verify --> git remote -v
8. To push publically(GitHub) --> git push -u origin master
9. For Fetching from github to local
		1.		fetch data into somewhere in .git --> git fetch origin master
				git merge origin/master
		
		
		2.		git pull origin master

10. Remove Connection between local and github repository --> git remote remove origin

11. git pull origin master --allow-unrelated-histories		

12. git clone url